apiVersion: v1
kind: ServiceAccount
metadata:
  name: argoplane-events-backend
  namespace: argocd
  labels:
    app.kubernetes.io/name: argoplane-events-backend
    app.kubernetes.io/part-of: argoplane
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: argoplane-events-reader
  labels:
    app.kubernetes.io/name: argoplane-events-backend
    app.kubernetes.io/part-of: argoplane
rules:
  - apiGroups: [""]
    resources: ["events"]
    verbs: ["list", "watch"]
  - apiGroups: ["events.k8s.io"]
    resources: ["events"]
    verbs: ["list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: argoplane-events-reader
  labels:
    app.kubernetes.io/name: argoplane-events-backend
    app.kubernetes.io/part-of: argoplane
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: argoplane-events-reader
subjects:
  - kind: ServiceAccount
    name: argoplane-events-backend
    namespace: argocd
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: argoplane-events-backend
  namespace: argocd
  labels:
    app.kubernetes.io/name: argoplane-events-backend
    app.kubernetes.io/part-of: argoplane
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: argoplane-events-backend
  template:
    metadata:
      labels:
        app.kubernetes.io/name: argoplane-events-backend
        app.kubernetes.io/part-of: argoplane
    spec:
      serviceAccountName: argoplane-events-backend
      containers:
        - name: backend
          image: argoplane-events-backend:dev
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8085
              protocol: TCP
          env:
            - name: PORT
              value: "8085"
            - name: LOG_LEVEL
              value: "info"
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8085
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8085
            initialDelaySeconds: 3
            periodSeconds: 5
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              memory: 128Mi
          securityContext:
            runAsNonRoot: true
            runAsUser: 65534
            allowPrivilegeEscalation: false
---
apiVersion: v1
kind: Service
metadata:
  name: argoplane-events-backend
  namespace: argocd
  labels:
    app.kubernetes.io/name: argoplane-events-backend
    app.kubernetes.io/part-of: argoplane
spec:
  ports:
    - port: 8085
      targetPort: 8085
      protocol: TCP
  selector:
    app.kubernetes.io/name: argoplane-events-backend
