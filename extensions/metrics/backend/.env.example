PORT=8080
PROMETHEUS_URL=http://kube-prometheus-kube-prome-prometheus.monitoring.svc:9090
LOG_LEVEL=info
# CONFIG_PATH=/app/config.json  # Optional: path to custom dashboard config (JSON). Omit to use built-in defaults.
