PORT=8081
LOG_LEVEL=info
VELERO_NAMESPACE=velero

# TLS configuration for fetching backup/restore logs from object storage.
# Use when object storage (e.g. MinIO) uses self-signed certificates.
# CA_CERT_PATH=/path/to/ca.crt
# INSECURE_TLS=true
